{
  "name": "gestionpro-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "json-server db.json --port ${PORT:-3001}"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.12"
  }
}
